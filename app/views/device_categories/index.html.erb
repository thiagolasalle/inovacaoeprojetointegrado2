<section class="jumbotron">
  <div class="container">
    <h1 class="display-3">Device Category</h1>
    <p>This is a template for a simple marketing or informational website. It includes a large callout called a jumbotron and three supporting pieces of content. Use it as a starting point to create something more unique.</p>
    <div class="float-right">
      <%= link_to 'New Device Category', new_device_category_path, class:"btn btn-success btn-lg" %>
    </div>
  </div>
</section>

<section class="container">
  <p id="notice"><%= notice %></p>
  <div class="container">
    <table class="table table-hover table-dark">
      <thead>
        <tr class="d-flex">
          <th scope="col" class="col-10">Name</th>
          <th scope="col" class="col-2"></th>
        </tr>
      </thead>
      <tbody>
        <% @device_categories.each do |device_category| %>
        <tr class="d-flex">
          <td class="col-10"><%= device_category.name %></td>
          <td class="col-2">
            <div class="row">
              <%= link_to 'Show', device_category, :class => "btn btn-info btn-sm mx-2" %>
              <%= link_to 'Destroy', device_category, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger btn-sm mx-2"  %>
            </div>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</section>